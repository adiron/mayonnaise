{
  "name": "@adiron/vectors",
  "version": "0.1.2",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/; tsc",
    "test": "echo Not implmented; exit",
    "lint": "eslint src -c ../../eslint.config.mjs",
    "prepublishOnly": "npm run build"
  }
}
